part of 'home_example.dart';

class MockHomeView extends StatelessWidget {
  const MockHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: EBHomeView(),
    );
  }
}
