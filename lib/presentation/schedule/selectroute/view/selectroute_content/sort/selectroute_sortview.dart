part of '../../selectroute_view.dart';

class _SelectRouteSortView extends StatelessWidget {
  const _SelectRouteSortView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sort'),
    );
  }
}
