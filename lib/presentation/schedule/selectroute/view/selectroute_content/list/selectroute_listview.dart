part of '../../selectroute_view.dart';

class _SelectRouteListView extends StatelessWidget {
  const _SelectRouteListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('List'),
    );
  }
}
