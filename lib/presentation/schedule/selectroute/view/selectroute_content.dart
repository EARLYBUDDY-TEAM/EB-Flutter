part of 'selectroute_view.dart';

class _SelectRouteContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('_SelectRouteContent'),
    );
  }
}
